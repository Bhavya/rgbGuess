module com.rgbguess.user {
    export class User {
        totalScore: number;
        scores: Array<number>;
        constructor() { }

        updateScore(score: number, timeStamp: number): void {

        }
    }
}
